# Core pinned environment for the classical-ml track (.venv, Python 3.14).
# Captured from the actual install on 2026-09-01 — the pre-flight currency check
# (CLAUDE.md) reviews these pins once per series; per-series extras (torch, xgboost,
# nltk, opencv-python, librosa, ...) are added and pinned when their series is authored.
numpy==2.5.2
pandas==3.0.5
pyarrow==25.0.1        # P1: pandas 3.0 uses pyarrow-backed strings when installed
scipy==1.18.1
statsmodels==0.15.0
scikit-learn==1.9.0
matplotlib==3.11.1
seaborn==0.13.2
jupyterlab==4.6.3
